# bullion-broker


For boolean-broker-backend, the repository structure is as mentioned below - 
Controller = "Take request -> forward -> return response"
Service = Business logic
DBInterface  = Everything to do with database
Model = What data looks like (Interface/Class definitions)
Common = All the common code that will be used across different modules